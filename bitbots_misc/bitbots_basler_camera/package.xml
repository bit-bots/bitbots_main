<?xml version="1.0"?>
<?xml-model href="http://download.ros.org/schema/package_format3.xsd" schematypens="http://www.w3.org/2001/XMLSchema"?>
<package format="3">
  <name>bitbots_basler_camera</name>
  <version>1.0.0</version>
  <description>This contains the interface between the Basler camera's pylon SDK and ROS2. In addtion to that is also applies some postprocessing (debayering, downsampling) to the images.</description>

  <maintainer email="bestmann@informatik.uni-hamburg.de">Marc Bestmann</maintainer>
  <maintainer email="info@bit-bots.de">Hamburg Bit-Bots</maintainer>

  <license>MIT</license>

  <author email="info@bit-bots.de">Hamburg Bit-Bots</author>

  <depend>bitbots_docs</depend>
  <depend>cv_bridge</depend>
  <depend>image_transport</depend>
  <depend>libopencv-dev</depend>
  <depend>rclcpp</depend>
  <depend>sensor_msgs</depend>
  <depend>camera_info_manager</depend>
  <depend>generate_parameter_library</depend>

  <buildtool_depend>ament_cmake</buildtool_depend>

  <export>
     <bitbots_documentation>
      <status>unknown</status>
    </bitbots_documentation>
    <build_type>ament_cmake</build_type>
  </export>
</package>
