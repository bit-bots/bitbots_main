# The name of this file shouldn't be changed, or else the Setup Assistant won't detect it
sensors:
  - "dummy"

# This is needed to suppress an error message during every launch
dummy:
  sensor_plugin: "~"
