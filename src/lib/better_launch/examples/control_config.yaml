/**:
  ros__parameters:
    update_rate: 1  # Hz
    
    # List of controllers
    joint_state_broadcaster:
      type: joint_state_broadcaster/JointStateBroadcaster

# Controller specific parameters
joint_state_broadcaster:
  ros__parameters:
    frame_id: my_awesome_frame
    joints:
      - joint1
      - joint2