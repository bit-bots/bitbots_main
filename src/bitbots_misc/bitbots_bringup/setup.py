from setuptools import setup

package_name = "bitbots_bringup"


setup(
    name=package_name,
    packages=[],
    data_files=[],
    install_requires=[
        "setuptools",
    ],
    zip_safe=True,
    keywords=["ROS"],
    license="MIT",
)
