% scale(1000) import("torso.stl");

// Append pure shapes (cube, cylinder and sphere), e.g:
translate([0,0,117])
cube([135, 150, 235], center=true);

// cylinder(r=10, h=10, center=true);
// sphere(10);
