#DoNothing
@ChangeAction + action:waiting, @LookForward, @Stand

#GetWalkreadyAndLocalize
@GetWalkready + r:false, @WalkForward + time:10, @Stand

#Init
@Stand + duration:0.1 + r:false, @ChangeAction + action:waiting, @LookForward, @Stand

-->BodyBehavior
$GameStateDecider
    INITIAL --> #Init
    READY --> $DoOnce
        NOT_DONE --> #GetWalkreadyAndLocalize
        DONE --> #DoNothing
