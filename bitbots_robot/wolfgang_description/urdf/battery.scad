% scale(1000) import("battery.stl");

// Append pure shapes (cube, cylinder and sphere), e.g:
translate([0,-20,0])
{
cube([140, 40, 44], center=true);
}
// cylinder(r=10, h=10, center=true);
// sphere(10);
